import Input from "./Input";

export default function PasswordInput() {
    return (
        <Input
            label="Contraseña"
            type="password"
            placeholder="********"
        />
    );
}